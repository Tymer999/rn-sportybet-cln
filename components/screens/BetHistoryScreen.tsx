import { ScrollView, Text, View } from 'react-native'
import React, { Component } from 'react'

export class BetHistoryScreen extends Component {
  render() {
    return (
      <ScrollView className="w-full h-full bg-secondary">
            </ScrollView>
    )
  }
}

export default BetHistoryScreen